# Keep rules for this app's release build.
# Library-specific rules (Compose, Room, DataStore, Navigation) ship as consumer rules.

# Keep line numbers in stack traces of release builds (optional, aids crash reports).
-keepattributes SourceFile,LineNumberTable
