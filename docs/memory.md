# Medication Reminder — session and verification memory

History and evidence only. Active rules live in `AGENTS.md`, `app/AGENTS.md`, and `README.md`; this file never overrides them.

## Commit timeline

| Commit | Date | What |
| --- | --- | --- |
| `2e6b4ec` | 2026-08-04 | v0.001 baseline: bootstrap of the single-module app (Room, photo store, schedules, time-zone modes, exact-alarm fallback, notification actions, receivers, dark M3 screens). |
| `5c94bb2` | 2026-08-05 | v0.002: usable version with basic features. |
| `3e59d19` | 2026-08-05 | v0.003. |
| `284f4b0` | 2026-08-23 | localization_proc_v0.001: English source + Simplified Chinese strings, DataStore `AppLanguage` preference, Settings selector; commit labeled "not verified". Reached master via the v0.005 merge. |
| `aaefe82` | 2026-08-23 | v0.003 (feature): persistent redacted lockscreen reminders with alias — `setOngoing(true)`/`setAutoCancel(false)`, `ReminderDismissReceiver` swipe resurrection, alias-based lockscreen title. Verified. |
| `8a6f9c7` | 2026-08-23 | v0.004: dose decisions deduped per schedule-zone local day. |
| `5057507` | 2026-08-23 | v0.005: merge of the lockscreen side branch (`284f4b0..8a6f9c7`) into master. |
| `eb8229b` | 2026-08-23 | v0.006: fix Activity owner lookup crash in `withAppLanguage` (`LocalizedContext` keeps the Activity in the base-context chain). |
| `794adc7` | 2026-08-23 | v0.007: move session history/verification evidence to `docs/memory.md`, slim root `AGENTS.md` and `README.md`, ignore generated `.kotlin/`. |
| `58c4f66` | 2026-08-23 | v0.008: release signing config (AGP 9 DSL, keystore.properties + env), R8 minify + resource shrink, `versionName 1.0.0-beta.1`, CI and release workflows. |
| `d52bbf8` | 2026-08-23 | v0.009: mark `gradlew` executable for CI. |
| `238c085` | 2026-08-23 | v0.010: setup-java v5, beta release history, README commit reference. |
| `84adb3f` | 2026-08-23 | Add MIT license (copyright `solo-race`); plain message outside the version format. |

| `cf7ae74` | 2026-08-24 | v0.011: fix localized-context crashes (time picker `BadTokenException`, exact-alarm `startActivity` exception) via resources-only ContextWrapper; refresh notifications/exact-alarm states on resume (`LifecycleResumeEffect`). Verified on device, debug + release. |
| `a84bce0` | 2026-08-24 | v0.012: align time/weekday formatting with app language — provide localized `LocalConfiguration`, per-call locale-aware formatters replace static `DateTimeFormatter`s. Verified EN/ZH on device. |
| `88afc03` | 2026-08-24 | v0.013: consistent Material icons in bottom navigation (`material-icons-extended`, BOM-pinned) replacing text glyphs that rendered at unequal sizes; all three icons now standard 24dp. User-confirmed on device. |
| `67c5e7f` | 2026-08-24 | v0.014: merge `codex/fix-localized-context-crashes` into master. |
| `3e43ef6` | 2026-08-24 | v0.016: drop lockscreen full-screen override; keep setAlarmClock scheduling. |
| `27d2382` | 2026-08-24 | v0.017: notification settings guidance, first-run permission dialog, alias label text. |
| `40afac8` | 2026-08-24 | v0.018: merge `codex/feature-notification-guidance` into master. |
| `4dc52ee` | 2026-08-25 | v0.019: Settings-gated self-heal — `MissedReminderReposter` re-posts undecided overdue reminders when any dose alarm fires; default off (`repost_missed_reminders`). |


## Beta release

- First beta `v1.0.0-beta.1` published 2026-08-23 as a GitHub release with the signed, minified `app-release.apk`; built by `.github/workflows/release.yml` (tag `v*` → signed `assembleRelease` → `softprops/action-gh-release`).
- Signing: local `release.keystore` + `keystore.properties` (both gitignored); CI gets `KEYSTORE_BASE64`, `KEYSTORE_PASSWORD`, `KEY_ALIAS`, `KEY_PASSWORD` repo secrets. Key alias `medsreminder`; key password equals store password (Windows keytool ignored `-keypass` — keep them equal if the keystore is regenerated).
- Second beta `v1.0.0-beta.2` published 2026-08-24 from tag on master `67c5e7f`; same workflow and signing path as beta.1. Contains v0.011–v0.014. CI run `32655679291`; CI-built APK installed and smoke-tested on device (Settings screen renders, nav icons equal size, 0 crashes).
- Third beta `v1.0.0-beta.3` published 2026-08-24 from annotated tag on master `b353615` (contains v0.016–v0.018); same workflow and signing path as beta.2. Release workflow run `32694140705`. CI-built APK downloaded and verified before release: signed by the release cert (CN=Meds Reminder, O=solo-race), 112 packaged files are code/libs/license metadata only — no database, media, or user data files and no medication names in any packaged content; `versionName` still `1.0.0-beta.1` (same convention as beta.2).

## Verification evidence

- 2026-08-04 session (v0.001 era): `testDebugUnitTest`, `assembleDebug`, `lintDebug` passed; 5 scheduling tests; lint 0 errors / 14 warnings; APK at `app/build/outputs/apk/debug/app-debug.apk`; Android Studio sync not confirmed (IDE first-run blocked it).
- 2026-08-23 session (v0.005 tree): full suite green via `java -cp gradle/wrapper/gradle-wrapper.jar org.gradle.wrapper.GradleWrapperMain testDebugUnitTest assembleDebug lintDebug` with `JAVA_HOME=D:\AndroidStudio\jbr`: 14 unit tests, 0 failures (`NextDoseCalculatorTest` 9, `ReminderRedactionTest` 5); lint 0 errors / 16 warnings; report `app/build/reports/lint-results-debug.html`.
- 2026-08-23 reconciliation: `codex/feature-persistent-notification` fast-forwarded `5c94bb2..5057507`; no code authored; conformance anchors spot-checked; device smoke blocked — OPPO not attached (adb kill/start/reconnect all empty).
- 2026-09-08 Phase 6 checkpoint for `codex/feature-medication-detail-dose-actions`: exact implementation/test HEAD `aec10609cbe4994e1f4f7ed1fbc9eb31ec2d2c1f` passed GitHub Actions CI Run #53 (`34167067003`) for unit tests, lint, and debug build. Focused feature coverage now includes stable unchanged-slot `doseTimeId` reconciliation; explicit occurrence parsing/validation; transactional first-write-wins Taken/Skipped; Room v2→v3 logical-day migration; persisted Taken and Skipped suppression across database close/reopen, metadata edit, schedule-zone change, and rebuild; decision-aware scheduler advancement; BOOT/MY_PACKAGE_REPLACED/TIME_SET/TIMEZONE_CHANGED reschedule wiring; receiver decision/fire race and stale occurrence suppression; exact notification occurrence routing; multiple daily slots; disabled/deleted medication or slot; and DST gap/overlap validation. Existing `NextDoseCalculatorTest` and reminder redaction tests remain in the green CI suite. No Phase 6 production-code change was required after the Phase 5 Review Gate. Device smoke for Home → Detail → Edit, notification → exact Detail, pre-alarm Taken/Skipped suppression, relaunch/metadata edit, next-occurrence alarm inspection, and long-lived Detail timing was **not executed in this checkpoint** because the automation environment had GitHub access only and no adb/device transport; the repository still records PLB110 as the physical target and no configured emulator. This is an explicit unavailable-device gap, not a reported pass.

- 2026-09-13 device verification for `codex/feature-medication-detail-dose-actions` @ `288d669` (PLB110 / ColorOS, in-place `install -r` over existing app data): `assembleDebug` BUILD SUCCESSFUL in 1 m 03 s (APK sha256 `cff3c76f…3925b836`), `testDebugUnitTest` BUILD SUCCESSFUL (18 classes / 80 tests / 0 failures / 0 skipped), install `Success` with `versionName=1.0.0-beta.1` and app data preserved (`user_version` stayed 3). Smoke: Detail rendering, Taken/Skipped persistence, edit-return recalculation, Home next-occurrence, alarms, and lifecycle passed; the notification entry path failed for an already-running app. Findings and dispositions: `docs/review/2026-09-13-2230-sgt-device-smoke-verification.md`. Full session log kept at `app/build/logcat-session.txt` and one screenshot at `app/build/screens/detail-final.png` (both inside the gitignored `app/build/`, so treat the review's quoted log lines as the durable evidence).

- 2026-09-13 warm-delivery correction for `codex/feature-medication-detail-dose-actions` (`5e28a09`, on `175b1ec`): `testDebugUnitTest assembleDebug lintDebug` **BUILD SUCCESSFUL** (59 actionable tasks, `--no-daemon --offline`), 87 tests / 19 classes / 0 failures / 0 errors / 0 skipped; lint 0 errors / 11 warnings; APK `app/build/outputs/apk/debug/app-debug.apk` 20,758,986 bytes, sha256 `99e86d5e…a9bd1`. Entry contract now: `MainActivity.applyIntent(intent, reapplied)` parses the extras once and is called by `onCreate` and by a new `onNewIntent` (which calls `super.onNewIntent` and `setIntent`); the parsed `ReminderEntry(medicationId, occurrence?)` goes to the Activity-scoped `MedicationViewModel` (`reminderEntry` = pending delivery, `reminderOccurrence` = occurrence Detail binds, `onReminderIntent` deduplicates only a re-applied launch intent by value, `reminderEntryOpened()` retires atomically and returns the retired entry, `clearReminderOccurrence()` for Home-opened Detail); the Compose effect navigates `Routes.detail(retired.medicationId)` with `launchSingleTop = true`; `ui/MedicationAppEntry.kt` (composition-local creation-time occurrence) was deleted. New Robolectric coverage `MainActivityReminderEntryTest` (8 tests) drives cold delivery, warm `ActivityController.newIntent` delivery, second-medication rebinding, `recreate()` retention without replay, re-applied-intent dedup, repeated delivery, supersede-before-open, and plain-launch fallback; the notification-path source-text assertion was removed from `MedicationDetailNavigationContractTest`. Independent code review found one MINOR consume/navigate mismatch (two deliveries in the same frame), fixed before the final gate and re-verified closed. Device smoke for plan scenarios 13–14 was **not executed** — no device transport this session. Review: `docs/review/2026-09-13-2340-sgt-reminder-entry-warm-delivery-correction.md`.

## Device smoke evidence (2026-08-23, PLB110 / ColorOS, feature-persistent-notification)

- Reminder lifecycle: 3 posted ids 101–103, flags `ONGOING_EVENT`, 2 actions (Taken/Skipped), channel `medication_reminders`. Swipe-dismiss resurrects via `deleteIntent` (user-observed). Taken/Skipped clear the tray and write dose events with `scheduledForEpochMillis` equal to the alarm's `origWhen`.
- Delete medication (UI): confirm dialog (Chinese on device locale) removes rows from `medications`, `medication_schedules`, `dose_times`, `dose_events`, cancels live alarms (0 remaining; residual `REMIND.` hits in `dumpsys alarm` are `*walarm*` history), and clears visible reminders via the mutation hook.
- ColorOS OEM behaviors (reported, no workarounds added per plan contingency):
  - `BOOT_COMPLETED` delivery: first `SKIPPED … reason: oplus startup`, later `DEFERRED … reason: mBroadcastConsumerDeferApply`; app process never started post-boot; no re-post. `MY_PACKAGE_REPLACED` same: `SKIPPED … reason: mBroadcastConsumerSkip`. Receiver re-post logic never executed on this device through OS paths; logic itself covered by `NextDoseCalculatorTest` (`mostRecentOverdueOccurrence`, 4 cases).
  - `am force-stop` clears the tray, cancels alarms, and sets the stopped state (blocks broadcasts until an explicit launch).
  - `am kill` refused to kill the process; `run-as … kill -9 <pid>` killed it and ColorOS cleared the tray without firing the delete intent. Process-death path not user-reachable.
  - Lockscreen: notifications never render on the 乐划锁屏 lockscreen and its pull-down is quick-settings only, despite `vis=PUBLIC` and `lock_screen_show_notifications=1` / `lock_screen_allow_private_notifications=1`. Lockscreen presentation is OEM-side.
- Scheduling on this device: inexact fallback alarm window is widened by ColorOS to 1h (`windowLength=3600000` in dumpsys); observed fires ~1–2 min after dose time. `install -r -g` is the only working grant path for POST_NOTIFICATIONS. A relaunch whose init lands after the dose time correctly schedules the next day — verify the alarm's `origWhen` epoch right after relaunch instead of waiting blind.
- App UI language on this device follows device locale (Chinese), contrary to the "default English" assumption; mixed-language evidence is expected on the localization branch, out of scope here.
- 2026-08-24 morning miss diagnosis: both 08:00 dose alarms (`REMIND.2`/`REMIND.4`) fired at 07:55:41 per uid wakeup stats and woke the process, but no reminder was seen. `dumpsys alarm` shows ColorOS widens even `setExactAndAllowWhileIdle` exact alarms (`exactAllowReason=permission`) to `windowLength=3600000`; receiver execution is additionally deferred past the dose time by cached-app freezing [INFERENCE]. All grants were valid at fire time (SCHEDULE_EXACT_ALARM granted, POST_NOTIFICATIONS granted USER_SET, channel HIGH/unblocked, zen off, ringer normal, app exempted bucket); no reboot overnight (uptime 14 h); app freshly installed 01:31 (`firstInstallTime`=`lastUpdateTime`). Presentation gap: 乐划锁屏 never renders notifications and heads-up banners need an already-on screen, so tray-posted reminders were invisible until unlock.
- 2026-08-24 notification-guidance branch verified on PLB110: Settings guidance card (锁屏显示提醒, between exact-alarms and privacy) with 打开通知设置 deep-linking to `com.oplus.notificationmanager` app page (允许通知/锁屏/横幅 toggles); alias label shows 别名/编辑通知消息 (zh) and "Alias / edit notification text" (en). First-run dialog (开启通知权限, 跳过设置/去设置) appeared after revoking POST_NOTIFICATIONS via the OEM master toggle (`pm revoke` OEM-blocked, SecurityException REVOKE_RUNTIME_PERMISSIONS); skip persisted across force-stop relaunch, and the dialog stayed gone after re-granting. All REMIND alarms kept `window=0` / `flags=0x9` "Alarm clock:" blocks after reinstall.
- 2026-08-24 setAlarmClock switch verified: unconditional `setAlarmClock` replacement (dumpsys: `window=0`, `flags=0x9`, "Alarm clock:" blocks on all REMIND alarms) fired the user-created TestMed on time at 12:05 with a visible notification. Full-screen intent and keyguard flags removed in v0.016: ColorOS 通知管理 锁屏/横幅 toggle renders reminders on 乐划锁屏 without exposing medication full-screen.
- 2026-08-25 20:00 night miss: REMIND.8 fired on time (`batterystats` wakeup ×1) and the receiver ran (alarm rescheduled to tomorrow, origWhen=1787745600000), POST_NOTIFICATIONS granted, self-heal toggle verified ON in DataStore — yet no reminder was ever seen and the tray ended empty. Day/night contrast: identical post path sticks for hours during daytime interaction but vanishes within minutes at night (screen off, oplus deepsleep.step wakeups active). Verdict: ColorOS night-time notification sweep [INFERENCE]; buffers rotate after ~40–80 min so the cancel caller is unprovable post-hoc. Known v0.019 boundary: self-heal runs only on dose-alarm fire and `mostRecentOverdueOccurrence` is same-day only, so a same-night sweep before any later dose loses that reminder until manual action.
- 2026-08-25 morning self-dismissal diagnosis + v0.019 verification (PLB110, wireless adb): user saw the 08:00 reminder vanish untapped; logcat/events buffers had rotated past morning. Forensics: process alive since ~01:00 (`batterystats`: 1 start, cached 12 h), zen off all night, no app-side cancel path, builder has no timeout/autoCancel → removal was system-side (ColorOS sweep while frozen-cached), and the deleteIntent resurrection broadcast never ran [INFERENCE from documented OEM skip behavior]. Installed build identified as yesterday's DEBUG-signed variant (apksigner cert match), not CI beta.3; updated via `install -r -g` debug build, data preserved. Suite green: 19 unit tests (`MissedReminderReposterTest` 5 new), lint 0 errors. Device end-to-end: toggle renders zh + persists across force-stop relaunch (DataStore key `repost_missed_reminders`); broadcast-firing doseTimeId 7 posted id 107 AND self-healed undecided doseTimeId 10 as id 110; decided doseTimeId 9 correctly untouched; force-stop cleared tray+alarms, relaunch restored all 4 REMIND alarms; toggle returned to OFF after test.

## Device smoke evidence (2026-09-13, PLB110 / ColorOS, medication detail dose actions)

Feature branch `codex/feature-medication-detail-dose-actions` @ `288d669`, debug build installed in place over existing app data (`user_version` stayed 3). Findings: `docs/review/2026-09-13-2230-sgt-device-smoke-verification.md`.

- Passed on device: Detail rendering (name/alias/dosage/note/schedule/zone/occurrence), Taken and Skipped persistence into `dose_events` (including `scheduledLocalEpochDay`), controls disabled after a decision, decision-aware alarm advancement and replacement, edit-return recalculation (weekday toggle preserves slot identity and keeps the decision suppressed; time change creates a new `doseTimeId` with no inherited decision), Home next-occurrence, background/foreground, rotation, cold relaunch after process death, deleted-medication `此药物已不存在。` state, History and Settings against the v3 schema.
- Failed on device: tapping a reminder while the app was already running did not navigate and lost the `open_*` occurrence. Device logs: `START u0 {… MainActivity (has extras)} … result code=3` (delivered to the existing activity) after `startActivity called from non-Activity context; forcing Intent.FLAG_ACTIVITY_NEW_TASK`; the app has no `onNewIntent`, so `onCreate`-only parsing never runs again. Cold delivery with the same extras (`LaunchState: COLD`) bound the occurrence correctly.
- OEM observations in this run: ColorOS refused the alarm-triggered receiver start for a not-running app (`OplusAppStartupManager: prevent start … ReminderReceiver` at the exact dose minute) and the one-shot alarm was consumed with no notification; killing the process also purged its posted reminders (`onUidGone … clear all notification`), which were later re-posted through the `ReminderDismissReceiver` deleteIntent path once the process ran again. ColorOS's grouped shade exposes no action-button nodes to the accessibility layer, so the notification's Taken/Skipped buttons were verified only structurally (`dumpsys notification`: both actions bound to broadcast PendingIntents).
- Not exercised: Room v2→v3 migration (device DB was already v3); reminder-tap-then-rotate; the notification action buttons by tap.

## Device environment

- Physical device: OPPO Find X8s+, ADB model `PLB110`, USB debugging enabled. Android SDK Google USB Driver installed; Windows uses `winusb.inf`. If `adb devices` stays empty, Android's OEM USB-driver guidance applies.
- v0.001-era smoke on PLB110: APK install/launch OK; dark home screen, add-medication fields (dosage, note, photo control, schedule controls), default three daily times, weekday/time-zone controls, save + persistence after force-stop/relaunch, Settings permission screen; logcat showed no crash entries.
- No emulator available: no AVD/system image configured.

## Tooling quirks

- The managed shell cannot exec `.\gradlew.bat` / `./gradlew` (Win32 error 193). Use the canonical PowerShell bootstrap in `AGENTS.md`, or the `java -cp` wrapper invocation above.
- With empty `JAVA_HOME`/`GRADLE_USER_HOME`, the wrapper may create its lock file under `C:\.gradle`; set both variables.

## Backlog

- Open (device re-verification for the medication-detail branch): re-run plan scenarios 13–14 on PLB110 — reminder opened while the task is alive (foreground and backgrounded), the same reminder opened twice, and rotation with the reminder Detail open — and confirm the OEM `onNewIntent` path renders `提醒对应服药` and applies Taken/Skipped to the reminded occurrence. The `onNewIntent` ingress, warm-path regression test, and the replacement of the source-text navigation contract are implemented in `5e28a09` (`docs/review/2026-09-13-2340-sgt-reminder-entry-warm-delivery-correction.md`); the 2026-09-13 22:30 finding is closed in code but the branch stays unmerged until that device run passes.
- Device-side reminder reliability on ColorOS: the alarm-triggered receiver start is blocked while the app is not running, and killing the uid purges posted reminders (2026-09-13 evidence). Mitigation is device-side allowlist/battery exemption; the in-app self-heal (`repost_missed_reminders`) can only run once the process is alive again.
- Device verification matrix: notification permission denial/grant, exact-alarm permission prompt, manual-zone travel prompt, 30-day history retention still open. Done on device: permission grant via `install -g`, inexact-alarm fallback, Taken/Skipped actions, force-stop behavior, reboot/package-replace recovery (OEM-blocked, see smoke evidence), delete-medication clearing, edit-return recalculation, rotation, cold relaunch, dose-boundary behavior (2026-09-13).
- Editor flows still unexercised on device: add/remove times, long notes, photo picker/camera/replacement/removal, enable/pause reminders, history review. Exercised: delete medication.
- Planned tests not yet written: Room, photo store, repository, scheduler DST/zone-change cases, Compose UI/instrumented.
- Lint: 16 non-blocking warnings to review; plan AGP 10 migration (external Kotlin / legacy DSL settings), add Android 12 data-extraction rules if backup behavior changes, raise `targetSdk` only after device testing.
- Android Studio project sync still unconfirmed end-to-end.
