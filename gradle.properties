org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
android.useAndroidX=true
kotlin.code.style=official
# AGP 9 defaults to built-in Kotlin. This project uses KSP and the Compose compiler plugin,
# so retain the external Kotlin Gradle plugin until those processors are migrated together.
android.builtInKotlin=false
android.newDsl=false
